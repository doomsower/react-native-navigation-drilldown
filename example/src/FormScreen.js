import * as React from 'react';
import FormScreenContent from './FormScreenContent';

const FormScreen = ({ navigation }) => (
  <FormScreenContent navigation={navigation} />
);

export default FormScreen;
